<Page x:Class="Online_Meeting.Client.Views.Pages.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Login" Background="#F5F7FA">

    <Page.Resources>
        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DFE4EA"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="CaretBrush" Value="#3498DB"/>
        </Style>

        <Style TargetType="PasswordBox" x:Key="ModernPasswordBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DFE4EA"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#3498DB"/>
            <Setter Property="Foreground" Value="#3498DB"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EBF5FB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="480"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side - Branding -->
        <Grid Grid.Column="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#2C3E50" Offset="0"/>
                    <GradientStop Color="#34495E" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="60">
                <!-- Icon -->
                <Border Width="100" Height="100" CornerRadius="20" Background="White" 
                        HorizontalAlignment="Center" Padding="20">
                    <materialDesign:PackIcon Kind="VideoAccount" Width="60" Height="60" 
                                             Foreground="#3498DB"/>
                </Border>

                <TextBlock Text="ONLINE MEETING" Foreground="White" FontSize="36"
                           FontWeight="Bold" HorizontalAlignment="Center" Margin="0,30,0,0"/>
                <TextBlock Text="Kết nối mọi lúc mọi nơi" Foreground="White"
                           FontSize="16" TextAlignment="Center" Opacity="0.85" Margin="0,10,0,0"/>

                <!-- Features -->
                <StackPanel Margin="0,50,0,0" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <materialDesign:PackIcon Kind="CheckCircle" Width="24" Height="24" 
                                                 Foreground="#2ECC71" VerticalAlignment="Center"/>
                        <TextBlock Text="Video call chất lượng cao" Foreground="White" 
                                   FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <materialDesign:PackIcon Kind="CheckCircle" Width="24" Height="24" 
                                                 Foreground="#2ECC71" VerticalAlignment="Center"/>
                        <TextBlock Text="Bảo mật P2P hiện đại" Foreground="White" 
                                   FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CheckCircle" Width="24" Height="24" 
                                                 Foreground="#2ECC71" VerticalAlignment="Center"/>
                        <TextBlock Text="Dễ dàng sử dụng" Foreground="White" 
                                   FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Right Side - Login Form -->
        <Border Grid.Column="1" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel VerticalAlignment="Center" Margin="50,40" MaxWidth="380">
                    <!-- Header -->
                    <TextBlock Text="Đăng nhập" FontSize="28" FontWeight="Bold" 
                               Foreground="#2C3E50" Margin="0,0,0,8"/>
                    <TextBlock Text="Nhập thông tin để tiếp tục" FontSize="14" 
                               Foreground="#7F8C8D" Margin="0,0,0,40"/>

                    <!-- Username Field -->
                    <TextBlock Text="Tên đăng nhập" FontSize="13" Foreground="#2C3E50"
                               FontWeight="Medium" Margin="0,0,0,8"/>
                    <Border BorderThickness="1" CornerRadius="6" BorderBrush="#DFE4EA" 
                            Background="White" Margin="0,0,0,20">
                        <Grid>
                            <materialDesign:PackIcon Kind="Account" Width="18" Height="18" 
                                                     Foreground="#95A5A6"
                                                     VerticalAlignment="Center" 
                                                     HorizontalAlignment="Left" Margin="12,0,0,0"/>
                            <TextBox x:Name="txtUsername" Style="{StaticResource ModernTextBox}" 
                                     Padding="40,10,12,10" BorderThickness="0"/>
                        </Grid>
                    </Border>

                    <!-- Password Field -->
                    <TextBlock Text="Mật khẩu" FontSize="13" Foreground="#2C3E50"
                               FontWeight="Medium" Margin="0,0,0,8"/>
                    <Border BorderThickness="1" CornerRadius="6" BorderBrush="#DFE4EA" 
                            Background="White" Margin="0,0,0,12">
                        <Grid>
                            <materialDesign:PackIcon Kind="Lock" Width="18" Height="18" 
                                                     Foreground="#95A5A6"
                                                     VerticalAlignment="Center" 
                                                     HorizontalAlignment="Left" Margin="12,0,0,0"/>
                            <PasswordBox x:Name="txtPassword" Style="{StaticResource ModernPasswordBox}" 
                                         Padding="40,10,12,10" BorderThickness="0"/>
                        </Grid>
                    </Border>

                    <!-- Remember & Forgot -->
                    <Grid Margin="0,0,0,25">
                        <CheckBox x:Name="chkRemember" Content="Ghi nhớ đăng nhập" 
                                  Foreground="#2C3E50" FontSize="13" 
                                  HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <Button Content="Quên mật khẩu?" Background="Transparent" 
                                BorderThickness="0" Foreground="#3498DB" FontSize="13" 
                                HorizontalAlignment="Right" Cursor="Hand" 
                                Click="ForgotPassword_Click" Padding="5,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <!-- Login Button -->
                    <Button Content="Đăng nhập" Style="{StaticResource PrimaryButton}"
                            Width="380" HorizontalAlignment="Stretch" Margin="0,0,0,15"
                            Click="LoginButton_Click" Height="44"/>

                    <!-- Register Link -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                        <TextBlock Text="Chưa có tài khoản?" Foreground="#7F8C8D" FontSize="13"/>
                        <Button Content="Đăng ký ngay" Background="Transparent" Foreground="#3498DB"
                                BorderThickness="0" FontSize="13" FontWeight="SemiBold" Cursor="Hand"
                                Padding="5,0" Click="RegisterButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                    <!-- Divider -->
                    <Grid Margin="0,0,0,25">
                        <Separator VerticalAlignment="Center" Background="#E8EAED"/>
                        <TextBlock Text="Hoặc tiếp tục với" Padding="12,0"
                                   FontSize="12" Foreground="#95A5A6" 
                                   HorizontalAlignment="Center" Background="White"/>
                    </Grid>

                    <!-- Social Login Buttons -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Google Button -->
                        <Button Grid.Column="0" Height="44" Background="White" 
                                BorderBrush="#E8EAED" BorderThickness="1" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" CornerRadius="6"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <materialDesign:PackIcon Kind="Google" Width="20" Height="20" 
                                                                     Foreground="#DB4437"/>
                                            <TextBlock Text="Google" Margin="8,0,0,0" Foreground="#2C3E50" 
                                                       FontSize="13" FontWeight="Medium" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F8F9FA"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Microsoft Button -->
                        <Button Grid.Column="2" Height="44" Background="White" 
                                BorderBrush="#E8EAED" BorderThickness="1" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" CornerRadius="6"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <materialDesign:PackIcon Kind="Microsoft" Width="20" Height="20" 
                                                                     Foreground="#00A4EF"/>
                                            <TextBlock Text="Microsoft" Margin="8,0,0,0" Foreground="#2C3E50" 
                                                       FontSize="13" FontWeight="Medium" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F8F9FA"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>